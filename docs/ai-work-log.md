# AI collaboration work log

Working notes for the brief-required `ai-collaboration-summary.md`. User input is paraphrased except where quoted; the checks and corrections are recorded so the final account can be reviewed against the work.

| Date | User input (paraphrased) | AI work and evidence |
| --- | --- | --- |
| 2026-09-24 | Asked for a senior-level Python take-home with all three phases, credential encryption from the initial design, and credible large-file behavior. Asked for intent and requirements docs, then supplied two outside reviews for critique. | Drafted and revised `intent.md` and `spec.md`; narrowed same-file behavior, set one-process SQLite scope, documented crash staging, error semantics, fixture setup, and measured-performance caveats. These docs predate Git initialization; commit `60dacac` records their state without pretending to show earlier revisions. |
| 2026-09-24 | Authorized isolated Docker/SFTP verification and asked what else was needed. | Tested the supplied image on Docker Desktop: upload, download, rename, and delete matched SHA-256. Verified amd64 emulation, changing host keys without persistence, and stable keys with a named `/etc/ssh` volume. Updated the spec; removed the disposable test project. |
| 2026-09-24 | Directed a separate repository with honest spec and guardrail commits; invited pushback on empty skeleton files. | Recommended executable scaffolding over empty placeholders. Created commits `21b1bf6` (supplied brief), `60dacac` (draft intent/spec), `757d090` (API contract), `0374290` (agent rules/evidence map), and `291027c` (runnable scaffold). Scaffold verification: Ruff clean, one pytest check passed, and live Uvicorn served `/openapi.json`. |
| 2026-09-24 | Authorized implementation and asked for deterministic requirement checks. | Commit `8cbdb41` added SQLite connections, Fernet encryption, local listing/staged writes, typed API errors, and tests. `make check` passed nine tests after this slice. SFTP connector work is in progress and has not yet passed integration. |
| 2026-09-24 | Asked to retain a trail for the evaluator-facing AI usage report. | Added this log and a small rule to keep it current in commit `6fe0998`. Real Docker integration exposed a Paramiko mode mistake: `xb` created a file without write permission; changing it to `wbx` made upload, readback, collision, overwrite, and restart tests pass. The SFTP slice passed 3 integration tests and landed in `b1d64cf`. |
| 2026-09-24 | Required deterministic proof that the connector contract is followed without type branching in copy logic. | Added a connector-neutral transfer service and SQLite transfer records. A third in-memory connector injected a failure on the second 1 MiB write; the record retained the first chunk count and the old destination. API tests covered same-file refusal, collisions, overwrite, and retrieval. Docker tests copied a binary file larger than one chunk both ways with byte equality and checked a server-down failed record. `make check`: 14 local tests; `make integration`: 5 Docker tests. |
| 2026-09-24 | Included Phase 3 and asked for low-friction, deterministic proof. | Added bounded CSV/JSON previews and sample-based type inference through the connector read contract. The supplied fixtures, malformed files, a record beyond the 1 MiB budget, and SFTP JSON were tested. A Pydantic field warning prompted an internal `schema_` alias while keeping the wire field `schema`; a JSON edge prompted full validation of small files even when only one row is requested. `make check`: 17 local tests; `make integration`: 6 Docker tests. |
| 2026-09-24 | Asked to include low-cost bonuses where they strengthen the connector story. | Added `POST /connections/{name}/healthcheck` using connector listing to test access without reading file contents. The Docker suite checks success, bad credentials, unreachable server, and missing remote root with distinct error codes. An explicit permission-denied case remains to verify. |
| 2026-09-24 | Approved addressing reviewer concerns, including interrupted transfers. | Added a Docker restart test that leaves a named unpublished stage file and a `running` record. On restart, the record becomes `failed`, the stage remains recoverable by transfer ID, and the SFTP API listing hides it. `make integration`: 7 Docker tests. |
| 2026-09-24 | Wanted useful bonuses without building a general test framework. | Added a single parameterized connector contract test for local and SFTP with four chunk-boundary sizes; it checks bytes, collision, overwrite, failed-stage cleanup, and path escapes. Added real SFTP listing-cap and unreadable-root healthcheck cases. `make check`: 21 local tests; `make integration`: 12 Docker tests before the unreadable-root case, which passed separately. |
| 2026-09-24 | Required credible large-file behavior or an informed limit estimate. | Initially thought root `data/` fixtures were missing; a Git check showed they were already tracked and byte-identical to `instructions/`, so no fixture commit was made. Added `make benchmark`, preflighted it at 1 MiB, then measured hash-matching 16 MiB and 256 MiB transfers both ways. In that first run, process peak RSS was 68.36 MiB after the largest case; the run used an amd64 SFTP container on an arm64 Docker engine. Saved raw JSON plus caveats and an explicitly untested 1 GiB estimate. |
| 2026-09-24 | Asked for an evaluator-friendly path and accurate API documentation. | Added `make smoke`, which starts a temporary live Uvicorn process and exercises connections, healthcheck, local/SFTP preview, two transfer directions, persisted status, and SHA-256 equality against Docker; it passed on the supplied 1005-byte CSV. Expanded the README with setup, main curl operations, design, limits, and evidence links. Strengthened the OpenAPI test to check the published routes and that the SFTP response schema omits the password. |
| 2026-09-24 | Required a submission-ready AI report and separate Git history. | Generated an initial 360-word response to the brief's exact prompt in `ai-collaboration-summary.md`, with three prompt-to-output examples and explicit AI/human authorship. Audited tracked paths and log statements for secrets. The README's `make run` initially hit the tooling sandbox's localhost-bind restriction; rerunning with the previously authorized local-port permission served `/openapi.json` with HTTP 200. GitHub publishing remained pending because `gh auth status` reported an invalid token. |
| 2026-09-24 | Asked for correctness without code/spec drift. | Final read-through found JSON numeric and boolean strings would be inferred like CSV text. Changed inference to respect native JSON string types while still recognizing ISO date strings; updated the spec and added an API test. |
| 2026-09-24 | Reported that the multiline README quick start copies with broken indentation and asked for one command. | Added `make quickstart` with a small shell script for dependency setup, one-time key and host-trust bootstrap, checks, live smoke, and server start. Repeated runs preserve `.env` and `known_hosts` rather than silently replacing trusted material. The README now presents one copyable line. It passed 21 local/API tests, 13 Docker tests, live smoke, and `/openapi.json` HTTP 200. A second run with the trusted host-key file temporarily removed exercised key bootstrap, reproduced the same saved keys at mode 600, and passed the same checks. The original local file was restored. |
| 2026-09-24 | Authorized pushing the separate repository and expected a submission-ready result. | Pushed the private GitHub repository. Its initial dependency alerts prompted an update to cryptography 50.0.1 and Paramiko 5.0.0. With the new lockfile, 21 local/API tests, 13 Docker tests, live smoke, and the full `make quickstart` path passed; the service returned HTTP 200 for `/openapi.json`. Re-ran the 16/256 MiB benchmark and replaced its raw and summarized evidence. Corrected the AI summary's stale claim that GitHub publication was blocked. |
| 2026-09-24 | Reported `INVALID_CONNECTION_SETTINGS` while using README examples, asked for live error logs with request parameters, and clarified that the example roots should not require shell variables or manual directory creation. | Removed `DATA_ROOT`/`OUTPUT_ROOT` assignments from the examples and used `data`/`output` paths directly. Local connection creation now creates a missing root and still rejects a file path or uncreatable directory. Added one private JSON request event per HTTP call with request ID, status, error code, and allowlisted parameters; passwords and raw bodies are excluded. Added `make logs`. `make check` passed 23 local/API tests, `make integration` passed 13 Docker tests, and `make smoke` passed. An isolated live server on port 8081 showed matching response/log request IDs for a 400 file-path error and a 201 auto-created directory. The user's running port-8080 server was not interrupted. |
| 2026-09-24 | Restarted the service and replayed the connection request so the live log could identify the submitted path. | The replay logged `POST /connections` with `"path":"string"`, a generated name, and status 201. This showed Swagger's generic placeholder had become an actual empty directory under the new auto-create behavior; the earlier shell-variable explanation was only an inference. Added named OpenAPI request examples for local data, local output, and the supplied SFTP fixture while preserving the discriminator. After verifying the placeholder was empty, the only stored connection, and unused by transfers, removed the directory and database row. `make check` passed 23 local/API tests; `make integration` passed 13 Docker tests. |
| 2026-09-24 | Showed that Swagger's `POST /transfers` form generated random connection names and `"string"` filenames and asked whether it was correct. | Confirmed that only `remote_server` existed in the live connection store, so the generated transfer request was not runnable. Added named upload/download requests with real connection names and fixture filenames, plus a completed-transfer success example and instructions to create connections first. Preserved the transfer request/response schemas. Left this and the earlier startup-banner edit uncommitted and unpushed at the user's request; both were committed later in `b43d62d`. |
| 2026-09-24 | Asked for a full review, held commits, and wanted to shape the AI account together; then authorized the resulting fixes. | Corrected OpenAPI's advertised validation/error response schema, fixed a complete CSV at the exact preview byte boundary, aligned the spec and setup recovery instructions with the implementation, and revised the AI summary for factual wording. `make check` passed 24 local/API tests, Docker integration passed 13 tests, and live HTTP smoke passed. The fixes were committed in `b43d62d` and the revised AI account in `5108cb0`. |
| 2026-09-25 | Asked Claude Code for a review of the whole repository with no changes, then asked it to address all findings; during the fixes, asked for a status update. | Claude Code ran a multi-agent review: 34 confirmed root causes and 14 hypotheses, one candidate refuted. On branch `fix/tribunal-findings` it implemented fixes test-first in focused commits (`25d6ccf` through the documentation commit): HTTP boundary and error envelope, single database owner, connector contract, local and SFTP hardening, transfer phases and progress, preview parsing, connection replace and delete, per-checkout Docker fixture, Ruff security rules, and strict mypy. Its own checks found issues the review had missed, all fixed with tests: Paramiko drops the status of pipelined writes still in flight at close; hostile SFTP replies could stall parsing; a source that shrinks mid-read was published as complete; a server could stall a worker by sending a byte at a time; FastAPI dropped null fields from the OpenAPI example. Mutation checks broke each new guard once; every mutation failed a test. When the candidate did not answer whether to add the stall watchdog, it took its stated recommendation. The branch was not pushed. |
| 2026-09-25 | Asked when the work would finish, then authorized a push, a pull request, and cleanup. | A second independent review of the fix branch found 5 medium and 10 low defects in the fixes. Claude Code fixed each test-first in `a8616fb`: an SFTP directory listing that never ends, a download sized by STAT before the file was opened, sync and close deadlines that did not grow with the upload, a late write failure recorded as a publication failure, a completion write and its read-back in separate transactions, and gaps in input validation (IPv6 scope IDs, host-name length, passwords that cannot be encoded, unknown transfer fields, bodies that are not UTF-8). Removing three of the new guards made tests fail. On `a8616fb`, `make quickstart` with an isolated fixture on port 2223 passed `make check` (230 tests, strict mypy, Ruff), `make integration` (14 tests), and `make smoke`; its final `make run` could not bind port 8080 because the candidate's own server held it. The isolated fixture and its generated files were removed afterwards. |
| 2026-09-25 | Ran `/gh-pr-enrich` on pull request #1 with its two Codex review comments, then asked to fix all of them and to test everything before pushing again. | Claude Code checked both comments against a snapshot of `870ea3c`. The UID 0 comment was right about the outcome and wrong about the cause: the fixture container starts, and its sshd refuses the UID 0 login (`PermitRootLogin no`), which a live probe of the image showed. The healthcheck comment was confirmed, and the local connector had the same defect. Fixes, each test-first: `8551a7a` checks probe removal in both connectors; `fc66c92` keeps the fixture user at UID 1001 on a root host and hands it `sftp_data`. Running `make quickstart` as root in Alpine (`docker:dind`) then found two more problems: the preview's JSON decoder overflowed a 128 KiB musl thread stack on deep nesting and killed the process (fixed in `3233b2f`), and a store test relied on file permissions that root ignores (fixed in `fc66c92`). On `3233b2f`: `make check` passed 242 tests on macOS; as root in Alpine, `make check` (237 passed, 5 skipped), `make integration` (14), `make smoke`, and `/openapi.json` passed; on macOS, `make integration` (14) and `make smoke` passed. |
| 2026-09-25 | Approved removing the `.env` reader's support for double-quoted values, which only an intermediate version of the branch wrote. | Claude Code removed it test-first: `read_env` now refuses a double-quoted value and names the key, `prepare_fixture_data` stops on that refusal, and quickstart reads the values it needs before starting the fixture. It first said the candidate's main checkout would need its `.env` rewritten; `main`'s quickstart wrote only an unquoted key, so that claim was corrected. `make check` passed, and a `make quickstart` run with a double-quoted `DATABRIDGE_KNOWN_HOSTS` stopped with the message before any container started. |

| 2026-09-25 | Asked Codex to recheck PR #1, then authorized fixes, a push, and resolving the review threads. | Independently reproduced seven current-head issues: SFTP client teardown after publication, stage/probe leaks after ambiguous OPEN, malformed LSTAT silently omitted from listings, JSON tail syntax misclassified as truncation, startup descriptor leaks, missing OpenSSH quickstart preflight, and stale benchmark evidence. Added fault-injection regressions and updated the connector, store, preview, quickstart, spec, and requirement evidence. Focused store/preview/tooling tests passed (50); loopback SFTP tests passed (64) with local socket access. The full `make check` gate passed Ruff, formatting, strict mypy, and 254 local/API tests; the disposable real Docker SFTP suite passed 14 integration tests; live HTTP `make smoke` logic passed both transfer directions with matching SHA-256. After fix commit `39a064e`, `make benchmark` passed 16/256 MiB and 16 MiB at 20 ms in both directions with matching hashes; current raw results and platform/emulation notes replaced the stale benchmark. |

Keep this log factual if further work lands. Keep passwords, encryption keys, and private environment details out of it.
