# Requirement evidence map

Status is **planned**, **partial**, or **verified**. Each row points to a test or artifact. `make check` runs static checks and local/API tests; `make integration` uses Docker SFTP; `make smoke` uses live HTTP and Docker; `make benchmark` measures large transfers.

| ID | Acceptance evidence | Status |
| --- | --- | --- |
| R1 | `test_local_connection_persists_and_lists_files` creates, lists, and reopens the same SQLite file; `test_local_connection_creates_missing_directory` proves a missing configured root is created; `tests/test_store.py` proves the owner lock refuses a second process and releases on exit, the schema version is enforced, unknown connection types fail closed, and new state files are owner-only; `tests/test_config.py` proves startup requires absolute paths; `test_connection_can_be_replaced_and_deleted` covers `PUT` (including a type change and a name mismatch) and `DELETE`; `test_unknown_fields_are_rejected`, `test_sftp_accepts_the_brief_user_field`, the host-name tests, and `test_text_settings_reject_control_characters_and_overlong_values` cover input validation Fault-injected owner-lock and store initialization tests prove descriptors close on chmod and unlock failures. | Verified |
| R2 | Local listing and caps: `test_local_connection_persists_and_lists_files`, `test_local_listing_reports_result_and_scan_caps`; SFTP listing and both caps: `test_sftp_roundtrip_collision_and_explicit_overwrite`, `test_sftp_listing_reports_result_and_scan_caps`; in `tests/test_sftp_connector.py`, `test_listing_stops_reading_at_the_scan_cap` proves enumeration stops at the scan cap, `test_listing_without_permission_bits_classifies_each_entry` lists only regular files when READDIR omits permissions, and `test_listing_skips_names_that_are_not_utf8` keeps one undecodable name from failing the listing `test_malformed_lstat_does_not_silently_omit_a_file` proves a protocol error fails the listing instead of returning incomplete success. | Verified |
| R3 | `test_supplied_csv_and_json_preview` checks the supplied 15-row CSV and six-object JSON through the local API; `test_preview.py` covers row and byte bounds, including a complete CSV exactly at the 1 MiB boundary, a budget filled from 7-byte short reads (`test_preview_fills_its_budget_from_short_reads`), and a record cut by the budget that is never returned (`test_csv_record_cut_by_the_byte_limit_is_never_returned`) `test_json_past_the_budget_tells_errors_from_truncation` includes invalid and incomplete tokens in the last preview bytes. | Verified |
| R4 | `test_sftp_roundtrip_collision_and_explicit_overwrite` creates a stored connection and lists/reads/writes through the connector against Docker | Verified |
| R5 | `test_api_transfers_binary_both_directions_and_preserves_collision` copies a binary file over a chunk in both directions and checks bytes; `tests/test_transfers.py` drives the same transfer code through a third, scripted connector; same-file and overwrite tests in `test_transfers.py` | Verified |
| R6 | `test_local_transfer_bytes_status_collision_and_overwrite`, `test_contract_violation_after_a_chunk_is_classified_by_phase`, `test_startup_marks_interrupted_transfer_failed`, `test_api_failed_sftp_transfer_has_retrievable_record`, and `test_interrupted_transfer_stage_stays_hidden_after_restart` check persisted outcomes, failure counts, destination preservation, recovery, and staging visibility. In `tests/test_transfers.py`, `test_each_failure_records_its_phase_code_and_side` and `test_missing_connection_names_its_side` prove each phase, code, and side is recorded and reported; `test_progress_is_checkpointed_at_most_once_per_interval` proves throttled progress; `test_publishing_is_recorded_before_publication`, `test_unrecorded_completion_is_reported_and_left_publishing`, and `test_source_close_failure_after_publication_still_completes` prove no record claims failure over a published file; `test_capacity_limit_rejects_extra_transfers_without_a_record` proves the concurrency cap; `test_transfers_are_listed_newest_first_and_filtered_by_status` covers `GET /transfers`. In `tests/test_store.py`, recovery of copying and publishing records, the state machine, typed record-write failures, and listing are tested. Seven guard mutations (recovery clause, finish-requires-publishing, terminal guard, progress throttle, capacity check, published flag, local hard-link publication) each fail the suite `test_client_teardown_failure_keeps_a_published_transfer_completed` proves the persisted record agrees with the published file. | Verified |
| R7 | `test_connection_errors_are_structured`, `test_sftp_failures_have_distinct_safe_codes`, `test_transfer_rejects_same_file_and_reports_missing_source`, and `test_api_failed_sftp_transfer_has_retrievable_record` check stable errors and transfer IDs. In `tests/test_app.py`, `test_unhandled_exception_returns_error_envelope_with_request_id` and `test_unknown_route_and_method_use_error_envelope` prove every non-2xx response uses the envelope and carries `X-Request-ID`; `test_validation_error_*` and `test_malformed_json_is_reported_as_such` check field-level problems; `test_request_log_records_error_params_without_secrets` and `test_request_log_rotates_and_keeps_every_file_private` check redaction, rotation, and owner-only log files; the Host and cross-site tests check the browser boundary. Against a loopback SFTP server with injected faults, `tests/test_sftp_connector.py` proves that a connection drop at each protocol step is `SFTP_UNAVAILABLE`, that missing roots, missing or non-regular files, and permission failures have distinct codes, that settings which cannot be encoded are `INVALID_CONNECTION_SETTINGS`, and that hostile replies (oversized, or with inflated counts), unrelated reply traffic, and a reply sent one byte at a time fail within seconds instead of holding the worker (`test_a_reply_trickled_byte_by_byte_is_abandoned`) The malformed-LSTAT regression retains a typed SFTP error rather than returning incomplete success. | Verified |
| R8 | `test_supplied_csv_and_json_preview`, `test_preview_inference_empty_mixed_and_malformed`, and `test_sftp_preview_uses_shared_parser` check types, empty/mixed values, malformed input, and SFTP parity; `test_type_inference_uses_ascii_grammars_and_strict_dates` rejects Python-only number forms, non-ASCII digits, and compact or week dates; the CSV tests cover 200 KB fields, blank lines, Unicode separators, and quoted newlines; `test_hostile_json_is_rejected_with_a_preview_error` proves deep nesting, unpaired surrogates, overflowing numbers, and oversized integers return preview errors, never 500, both for complete files and past the budget. `test_deep_json_is_refused_on_a_small_thread_stack` previews 100,000 levels of nesting in a thread with a 128 KiB stack, the musl default, and gets `PREVIEW_LIMIT_EXCEEDED` instead of a process crash; `test_depth_is_checked_in_every_row_of_a_complete_file` covers a deep row after the returned ones. Four guard mutations (read loop, record cut, blank-line skip, depth limit) each fail the suite, and so does removing either nesting check that runs before the decoder The JSON boundary test distinguishes a malformed tail token from a cut literal. | Verified |
| R9 | `test_local_connection_persists_and_lists_files` and `test_healthcheck_reports_a_read_only_root` check a writable and a read-only local root and that the probe leaves no file; `test_sftp_failures_have_distinct_safe_codes` and `test_sftp_healthcheck_reports_unreadable_root` check SFTP bad password, unavailable server, missing root, and unreadable root against Docker; `tests/test_sftp_connector.py` repeats the SFTP cases without Docker and adds a read-only root (`check_access` reports not writable). A probe that cannot be removed fails the check and names the probe: `test_a_probe_that_cannot_be_removed_fails_the_access_check` in `tests/test_sftp_connector.py` (REMOVE denied) and `tests/test_local_connector.py` (unlink refused), and `test_connection_drop_is_unavailable` drops the SFTP connection before the directory open, the probe open, and the probe REMOVE `test_open_reply_failure_removes_a_created_hidden_file[check]` proves probe cleanup even when OPEN fails after creation. | Verified |
| R10 | `make benchmark`, [raw results](docs/benchmark-results.json), and [measurement notes](docs/benchmark.md): 16/256 MiB both ways, matching SHA-256, end-to-end duration, throughput, peak RSS, platform/emulation, a 16 MiB case at an added 20 ms round trip, and an explicitly untested 1 GiB estimate, measured on commit `3fe79dd`. A one-off run with one request in flight ([raw](docs/benchmark-one-request-results.json)) shows what pipelining changes: 1.2 MiB/s instead of 16–20 MiB/s at 20 ms. `test_round_trip_pipelines_reads_and_writes` proves requests are pipelined in both directions, and `test_relay_forwards_sftp_with_added_round_trips` proves the benchmark relay adds the delay | Verified |
| R11 | `test_connector_contract_at_chunk_boundaries` runs identical empty/boundary-size read/write, collision, overwrite, cleanup, and escape cases against the local connector, a loopback SFTP server (no Docker), and the Docker SFTP server; other tests cover local symlink escape and publish-time SFTP collision; `tests/test_local_connector.py` covers the connector registry, reserved and 255-byte names, non-regular files (FIFO, directory, device), symlink loops, the local publish-time race, mode preservation on overwrite, classified write failures, filesystems without hard links, cleanup errors after publication, and sources that grow or shrink while read; `tests/test_sftp_connector.py` proves nothing is published after a failed close, a failed pipelined write (collected while writing or when finishing), a server-truncated stage, or a server without `posix-rename@openssh.com`, that a lost publish reply is reported as a possible publication, that `fsync@openssh.com` is requested and optional, that an overwrite keeps the destination mode, and that a successful write removes nothing `test_open_reply_failure_removes_a_created_hidden_file[write]` proves stage cleanup after an ambiguous OPEN reply. | Verified |
| R12 | `test_openapi_documents_reachable_statuses_and_error_codes` compares every operation's documented statuses with the statuses it can return, and checks the error-code enum, the discriminated connection schema, concrete connection and bidirectional transfer examples, the completed-transfer 201 example with every record field (nulls included), date-time timestamps, the transfer status enum, and the redacted output schema; `make smoke` fetches live OpenAPI; README links Swagger UI and gives copyable curl examples | Verified |
| R13 | Ciphertext/redaction and wrong/missing key in `test_connections.py`; `test_non_ascii_key_error_does_not_echo_the_key` proves a malformed key never appears in the startup error; SFTP use after store restart in `test_sftp_roundtrip_collision_and_explicit_overwrite`; the host-trust tests in `tests/test_sftp_connector.py` prove the server receives no authentication attempt when the known-hosts file is missing or malformed or the host key is unknown or changed; the request-log test proves a submitted password is absent from JSON-line events; tracked-file and logging audit found no committed key, known-hosts file, SQLite database, or secret-bearing log statement | Verified |
| R14 | `make smoke` started Uvicorn, exercised local/SFTP connections, healthcheck, local and remote previews, both transfer directions, persisted status, and SHA-256 equality (1005-byte supplied CSV). `make quickstart` ran the complete reviewer path twice, including once with a missing trusted host-key file, and served `/openapi.json` with HTTP 200. `tests/test_tooling.py` proves the fixture trust script scans once, trusts `localhost` with the keys already trusted for `127.0.0.1`, and adds nothing on a rerun; `tests/test_env_file.py` proves `.env` values are stored and read literally and a double-quoted value is refused, and a `make quickstart` run with a double-quoted `DATABRIDGE_KNOWN_HOSTS` stopped with that message before any container started; `test_sftp_localhost_alias_is_trusted` connects through `localhost`. On commit `3fe79dd`, `make quickstart` ran again with a fresh `.env` that set `DATABRIDGE_SFTP_PORT="2223"`: it started its own Compose project on that port, trusted both loopback names, and passed `make check`, `make integration`, and `make smoke`; its final `make run` could not bind port 8080 because another DataBridge server already held it, so that last step was not re-observed. As root, a UID 0 fixture user is refused at login (`PermitRootLogin no`; a live probe of the image confirmed it), so quickstart keeps UID 1001 and gives `sftp_data` to it: `tests/test_sftp_fixture_user.py` covers the UID choice, the hand-over, and a refused stored UID of 0. `make quickstart` then ran as UID 0 on a native Linux filesystem inside a `docker:dind` container (Alpine 3.24, musl, aarch64): it stored `DATABRIDGE_SFTP_UID='1001'`, gave `sftp_data` to UID 1001, passed `make check` (237 passed; the 5 file-permission tests skip under root), `make integration` (14 passed), and `make smoke`, and `make run` served `/openapi.json` with HTTP 200 `test_quickstart_reports_missing_ssh_keyscan_before_setup` proves OpenSSH preflight stops before changing local setup. | Verified |

Passing local tests alone does not establish the Docker or live HTTP path. The integration suite and smoke command cover those separately. Benchmark measurements apply to the stated machine and fixture.

A second, independent review of the fix branch found 5 medium and 10 low defects in the fixes themselves; each now has a test that failed first: a directory that never ends (`test_a_directory_that_never_ends_is_bounded`), a file replaced between STAT and OPEN (`test_read_uses_the_size_of_the_file_it_opened`), a late write failure recorded as publication (`test_a_failed_tail_write_is_a_write_failure_not_a_publication`, `test_each_failure_records_its_phase_code_and_side`), sync and close deadlines (`test_sync_and_close_get_time_in_proportion_to_the_upload`), a body that is not UTF-8 (`test_body_that_is_not_utf8_is_a_request_error`), and the low items in `tests/test_env_file.py`, `tests/test_local_connector.py`, `tests/test_store.py`, `tests/test_preview.py`, `tests/test_connections.py`, and `tests/test_app.py`.

## Recorded gate runs

| Date | Commit | `make check` | `make integration` | Live checks |
| --- | --- | --- | --- | --- |
| 2026-09-24 | `583fa38` | Ruff, 21 tests | 13 passed | `make smoke`, `make benchmark` (16/256 MiB both ways), and `make quickstart` with `/openapi.json` HTTP 200 |
| 2026-09-24 | `eedb23f` | Ruff, 23 tests | 13 passed | `make smoke`; a temporary server on port 8081 logged a 400 with the submitted path and a 201 for a created directory, each with its `X-Request-ID` |
| 2026-09-24 | `4f97f9c` | Ruff, 23 tests | 13 passed | — |
| 2026-09-24 | `b43d62d` | Ruff, 24 tests | 13 passed | `make smoke` |
| 2026-09-25 | `3fe79dd` | Ruff (with security rules), strict mypy, 203 tests | 14 passed | `make quickstart` on port 2223 through `make smoke`; `make benchmark` (16/256 MiB and 16 MiB at 20 ms, both ways) |
| 2026-09-25 | `a8616fb` | Ruff (with security rules), strict mypy, 230 tests | 14 passed | `make quickstart` on port 2223 through `make smoke`; its final `make run` could not bind port 8080, which another DataBridge server held; `make benchmark` not rerun |
| 2026-09-25 | `3233b2f` | Ruff (with security rules), strict mypy, 242 tests (235 at `8551a7a`, 239 at `fc66c92`) | 14 passed | macOS: `make quickstart` on port 2223 through `make smoke` and `make run`, then `make integration` and `make smoke` again on `3233b2f`. As UID 0 in `docker:dind` (Alpine 3.24, musl, aarch64) on `3233b2f`: `make quickstart` passed `make check` (237 passed, 5 skipped under root), `make integration` (14), and `make smoke`, and `/openapi.json` returned HTTP 200; `make benchmark` not rerun |
